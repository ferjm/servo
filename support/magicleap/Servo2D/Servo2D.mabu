KIND = program

INCS = \
    code/inc/ \
    code/inc.gen/

SRCS = \
    code/src/main.cpp \
    code/src/Servo2D.cpp

LIBPATHS.debug = \
    ../../../target/aarch64-linux-android/debug \
    ../../../target/gstreamer/gst-build-arm64-v8a

LIBPATHS.release = \
    ../../../target/aarch64-linux-android/release \
    ../../../target/gstreamer/gst-build-arm64-v8a

STLIBS = \
    mlservo

SHLIBS = \
    log \
    z \
    gstreamer_android

USES = \
    lumin_runtime \
    code/srcsGen
